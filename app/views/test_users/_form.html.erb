<%= form_for(@test_user) do |f| %>
  <% if @test_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_user.errors.count, "error") %> prohibited this test_user from being saved:</h2>

      <ul>
      <% @test_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%=  select_tag(:role, options_for_select([['Tutor', 'admin'], ['Student', 'student']],'student')) %>
  
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
